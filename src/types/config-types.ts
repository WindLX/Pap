export interface ISystemConfig {
    host: string
    port: number
}

export interface IBasicConfig {
    title: string
    log_level: string
}

export interface IPathConfig {
    note_dir: string
    log_path: string
    tag_path: string
    emoji_path: string
}
